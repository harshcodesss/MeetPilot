Drop the three carousel videos here (referenced by src/components/auth/LoginCarousel.tsx):
  capture.mp4     — Subsystem 1, live caption capture
  extraction.mp4  — Subsystem 3, action-item extraction
  automation.mp4  — Subsystem 4, drafted actions
Until present, each slide falls back to its brand gradient.
